import React from 'react';

interface Section2Props {}

const displayName = 'Section2';

export const Section2 = (props: Section2Props) => {
  const {} = props;

  return (
    <div className="bg-background h-screen bg-cover bg-center"></div>
  );
};

Section2.displayName = displayName;
